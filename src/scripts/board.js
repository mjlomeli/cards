class Board {
    constructor(){


    }
}


export default Board


var main = function(){
    // code to run

    var board = Board();
}

if (typeof require !== 'undefined' && require.main === module) {
    // same as if __name__ === "__main__"
    main();
}